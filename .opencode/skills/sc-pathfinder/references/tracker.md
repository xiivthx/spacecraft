> Consult when: creating child issues, setting labels, wiring blocking edges, or querying the frontier on the project's issue tracker. Use this to understand tracker-specific operations.

## Spacecraft integration

Pathfinding maps and tickets live on the repo's issue tracker (GitHub Issues by default, or a local-markdown tracker). This reference documents the tracker-specific API — creating child issues, applying labels (`wayfinder:map`, `wayfinder:research`, `wayfinder:prototype`, `wayfinder:grilling`, `wayfinder:task`), wiring blocking dependencies, and querying the frontier (open, unblocked, unclaimed children).

### Default tracker: GitHub Issues

TODO: document GitHub Issues API operations for wayfinding — creating issues with parent/child relationships, labeling, blocking via "blocked by" references, and querying the frontier with GitHub's GraphQL API.
